.catalog:
  name: "Rundeck"
  version: 0.0.1
  description: Rundeck
  questions:
    - variable: "rundeck_url"
      type: string
      required: true
      label: "Rundeck URL"
      description: "<scheme>://<hostname> for the 302 redirect"
    - variable: "consul_encrypt"
      type: string
      required: false
      label: "Consul encryption key"
      description: "Generated with `consul keygen`"
    - variable: "consul_url"
      type: string
      required: false
      label: "Consul server URL"
      description: "<host>:<port> of Consul server"
    - variable: "consul_encrypt"
      type: string
      required: false
      label: "Consul encryption key"
      description: "Generated with `consul keygen`"
artifactory:
  scale: 1

